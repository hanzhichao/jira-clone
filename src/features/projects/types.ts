export type Project = {
  $id: string;
  id: string;
  name: string;
  key?: string;
  description?: string;
  imageId?: string;
  imageUrl?: string;
  workspaceId: string;
  createdAt: string;
  updatedAt: string;
};
